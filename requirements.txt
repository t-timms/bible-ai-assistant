# Bible AI Assistant - Python Dependencies
# Install in conda env: pip install -r requirements.txt
# PyTorch: install separately (nightly with CUDA 12.8+ for RTX 5070 Ti). See docs.
#
# Training + RAG in one env (e.g. bible-orpo): use transformers 5.x for native Qwen3.5 (qwen3_5).
# Legacy Qwen3-only stacks used transformers<=4.57.2; this project targets Qwen3.5 + Unsloth on 5.x.

# Training
transformers>=5.1.0
datasets>=2.18.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.33.0
bitsandbytes>=0.44.0
unsloth>=2024.12.0
wandb>=0.18.0
huggingface_hub>=0.20.0
sentencepiece>=0.2.0

# RAG
chromadb>=0.4.0
sentence-transformers>=2.2.0
einops>=0.7.0
rank_bm25>=0.2.2
numpy>=1.24.0

# API and serving
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0

# UI
gradio>=4.0.0

# Voice (STT + UI)
faster-whisper>=1.0.0
soundfile>=0.12.0

# Utilities
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0
