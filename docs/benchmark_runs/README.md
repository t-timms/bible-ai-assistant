# Benchmark run artifacts

JSON outputs from **`scripts/run_benchmark.py`** (or `training/evaluate.py` with `--output`).

- Filename pattern: `YYYYMMDD_<label>_<keyword|judge>.json`
- Commit small result files if you want history in git; or keep local only.

See **[BENCHMARK_PROTOCOL.md](../BENCHMARK_PROTOCOL.md)** for the protocol definition.
